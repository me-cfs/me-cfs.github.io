# frozen_string_literal: true

source "https://rubygems.org"

# This is the default setup for GitHub Pages
gem 'github-pages', group: :jekyll_plugins

group :jekyll_plugins do
  gem 'jekyll-sitemap'
end
